language: 
  - node_js

# Version number
node_js: 
  - 4.2.1

build:
  ci: 
    - node --version
    - echo "done success"

integrations:
  notifications:
    - integrationName: jatin-webhook
      type: webhook
      payload:
        - repo_name=$REPO_FULL_NAME
        - commit_message=$COMMIT_MESSAGE
      on_success: always
      on_failure: always
